# Path to the SQLite database file. Defaults to ./travel_planner.db in the
# project root. Point this at a persistent, writable volume in production.
# File-based SQLite is NOT suitable for serverless/ephemeral filesystems —
# use a managed/networked database there instead.
# DATABASE_PATH=/data/travel_planner.db
